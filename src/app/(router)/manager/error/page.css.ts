import { style } from '@vanilla-extract/css';

export const row = style({
  marginBottom: '10px',
});

export const dateCol = style({
  width: '160px',
});

export const messageCol = style({
  width: '900px',
  maxWidth: '900px',
});
