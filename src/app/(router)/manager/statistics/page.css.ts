import { style } from '@vanilla-extract/css';

export const searchRow = style({
  borderBottom: '1px solid #eee',
  padding: '10px 0',
});

export const filterRow = style({
  marginLeft: '10px',
  color: '#666',
});

export const pageRow = style({
  marginLeft: '10px',
  color: '#0070f3',
});

export const tagItem = style({
  marginRight: '8px',
});

export const lastVisit = style({
  fontSize: '12px',
  color: '#999',
});
