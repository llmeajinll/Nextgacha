import React from 'react';
import { Range } from '@/shared/ui';
import { divider } from './productInfo.css';

export default function ProductInfo() {
  return <Range width='full' className={divider}></Range>;
}
