import { style } from '@vanilla-extract/css';

export const section = style({
  width: '1272px',
  padding: '0 20px',
  marginBottom: '10px',
});

export const sectionTitle = style({
  marginBottom: '20px',
});
